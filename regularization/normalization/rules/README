cn_num_words_dict:
	词典中带有数字的词，不做转换

cn/cn_units:
	-第一列：符号；第二列：单位名称
	-符号在数字之后，转换后单位仍然在数字之后（eg: 100m -> 一百米）

cn/cn_units_need_change:
	-第一列：符号；第二列：单位名称
	-“======”以上的表示符号在数字之前，需要将单位名称放到数字之后（eg: ￥123 -> 一百二十三元）
	-“======”以下的表示符号在数字之后，需要将单位名称放到数字之前（eg: 100% -> 百分之一百）

en同cn